# Important! Env Only

# tomcat config
server.tomcat.basedir=tomcat
server.tomcat.accesslog.directory=logs
server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.pattern=common
server.tomcat.accesslog.prefix=access
server.tomcat.accesslog.suffix=

# spring.datasource.url=${DB_RIMBAUD_URL}?useUnicode=true&characterEncoding=utf8&useSSL=false
# spring.datasource.username=${DB_RIMBAUD_USERNAME}
# spring.datasource.password=${DB_RIMBAUD_PASSWORD}

# spring.redis.host=${RIMBAUD_REDIS_URL}
# spring.redis.password=${RIMBAUD_REDIS_PASSWORD}
